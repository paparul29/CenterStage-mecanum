package org.firstinspires.ftc.teamcode.commands;

import com.arcrobotics.ftclib.command.CommandBase;
import com.arcrobotics.ftclib.command.CommandOpMode;
import com.arcrobotics.ftclib.command.InstantCommand;
import com.arcrobotics.ftclib.command.SequentialCommandGroup;
import com.arcrobotics.ftclib.command.WaitCommand;
import com.arcrobotics.ftclib.command.WaitUntilCommand;

import org.firstinspires.ftc.teamcode.subsystems.Intake;
import org.firstinspires.ftc.teamcode.subsystems.PixelHolder;

public class Sensores extends SequentialCommandGroup {
/*
    Intake intake;


    public Sensores(Intake intake, PixelHolder pixelHolder){

        addCommands(
                new WaitUntilCommand(intake::grabPixel1S),
                new WaitCommand(500),
                new InstantCommand(pixelHolder::pixel1Hold),
                new InstantCommand(pixelHolder::pixel2Hold)
        );
    }


 */

}
